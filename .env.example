# Confirmed public production origin. Overrides must remain origin-only HTTPS URLs.
NEXT_PUBLIC_SITE_URL=https://ctrlctrlx.top

# Optional Vercel KV settings for visitor counts and chat rate limiting.
KV_REST_API_URL=
KV_REST_API_TOKEN=
