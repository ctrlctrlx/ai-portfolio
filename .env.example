# Optional public production origin. Use only an origin-only HTTPS URL.
NEXT_PUBLIC_SITE_URL=

# Optional Vercel KV settings for visitor counts and chat rate limiting.
KV_REST_API_URL=
KV_REST_API_TOKEN=
