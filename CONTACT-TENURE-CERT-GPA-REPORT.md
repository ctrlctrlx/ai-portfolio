# 首页按钮跳转 · 班长任期 · 证书时间 · 教育精简 报告

- 仓库：`E:\PRD\my-ai-portfolio`（Next.js 16 App Router + TS + 双语 zh/en）
- 分支：`refactor/streamline-profile-content`
- 范围：首页「联系我」改为站内跳转、班长任期统一、计算机二级取证时间补全、删除专业排名并弱化 GPA
- 状态：**全部通过**（`npm run verify` content / profile / chat / resume / lint / typecheck / build 全绿；生产构建 **28/28**）
- 本次未创建任何 git commit / tag / branch / push

---

## 0. 结论摘要

| 验收项 | 实测结果 |
| --- | --- |
| 首页「联系我」跳转 | ✅ 中英首页均指向 `/[lang]/contact`；hero 内 `mailto:` 数量为 **0**；无 `target="_blank"`（当前页跳转） |
| 与导航栏一致 | ✅ 两者均为 `href="/zh/contact"`，且按钮不再带 mailto 语义的 `aria-label` |
| 按钮样式/图标/文字 | ✅ 类名、`var(--card-border)`/`var(--foreground)`、`Mail` 图标与「联系我 / Contact」文案均未改动 |
| 班长任期 | ✅ 全站唯一数据源 `about.ts` 已改为 `2019.09 – 2022.12`；`/zh/about`、`/en/about` 实测命中，旧任期 `2020.09 – 2023.06` 残留 **0** |
| 机器人职务信息 | ✅ 实践经历回答输出 `• 班级班长（2019.09 – 2022.12）` |
| 计算机二级取证时间 | ✅ `credentials.ts` 新增 `year: "2021.03"`；`/zh/honors`、`/en/honors` 均显示 |
| 机器人证书信息 | ✅ 证书回答输出 `• 全国计算机等级考试二级（C 语言程序设计）（2021.03）` |
| 删除专业排名 | ✅ 两校 GPA 改为 `3.6/4.0` / `3.9/4.0`；首页、关于页、简历页实测 `4/19`、`1/200` 残留 **0** |
| 机器人教育回答 | ✅ 无排名信息（且本就不含 GPA，仅学历三要素） |
| GPA 视觉弱化 | ✅ 渲染为 `<span style="color:var(--muted)"> · GPA 3.6/4.0</span>`，同字号、不加粗 |
| 英文页 | ✅ 6 条 `/en` 路由可见文本汉字 0、中文标点 0 |
| `npm run verify` | ✅ 全链路通过 |
| 生产构建 | ✅ 28/28 静态页生成成功 |

---

## 1. 一处需确认事项的处理结果

任务 2.2 要求同步「简历页『校园经历/学生工作』板块」，但**简历页没有该板块**（现有板块仅：教育经历 / 项目经历 / 核心技能 / 荣誉奖项 / 专利）。你的答复是「正确板块为：实践经历」——该板块位于 **`/[lang]/about` 的「实践经历」**。因此：

- ✅ 已同步：关于页「实践经历」+ 机器人知识库/回答
- ⛔ 未新增简历页板块（未创建新板块，简历维持 5 个板块与既有 A4 打印篇幅）

---

## 2. 修改文件清单与每处改动说明

| 文件 | 改动 |
| --- | --- |
| `app/[lang]/page.tsx` | 首页首屏「联系我」由 `<a href={mailto:…}>` 改为 `<Link href={\`/${locale}/contact\`}>`；同步移除该按钮原有的 mailto 语义 `aria-label`（改后与导航栏入口完全一致），并清理不再使用的 `getContactHref` / `getPublicContact` 导入与 `publicEmail` 派生 |
| `src/data/profile/about.ts` | 本科「班级班长 / Class Monitor」的 `period` 由 `2020.09 – 2023.06` 改为 `2019.09 – 2022.12`（中英一致，统一使用 en dash） |
| `src/data/profile/credentials.ts` | 「全国计算机等级考试二级（C 语言程序设计）」新增 `year: "2021.03"`（与全站 YYYY.MM 口径一致） |
| `src/data/profile/education.ts` | 两校 `gpa` 去掉排名：`3.6/4.0（4/19）` → `3.6/4.0`；`3.9/4.0（1/200）` → `3.9/4.0`；注释同步说明「仅保留绩点，不展示专业排名」 |
| `src/data/profile/types.ts` | `EducationEntry.gpa` 注释更新（不再提专业排名）；`PracticeEntry.period` 示例改为 `2019.09 – 2022.12` |
| `src/lib/career-agent.mjs` | ① 新增 `buildPractice`（实践经历/学生工作，含职务任期）与 `buildCredentials`（证书资质，含取证时间）两条规则；② RAG 上下文新增「实践经历（含学生工作）」与「证书资质」两个区块，使模型也掌握这两类公开数据；③ 「证书资质」规则**置于「荣誉资质」之后**（见 §4.1） |
| `src/lib/career-agent.d.mts` | `CareerCorpus` 新增 `credentials?: Credential[]` |
| `app/api/chat/route.ts` | 语料加入 `publicCredentials` |
| `scripts/verify-chat-contract.mjs` | 语料加入 credentials；新增断言：实践经历回答必须含每条条目的「名称 + 起止时间」、教育回答不得出现 `(n/m)` 形式的排名、证书回答必须含证书名与取证时间；快捷问题清单新增 4 条（学生工作×2、证书×2）→ 26 条 |
| `scripts/verify-profile-data.mjs` | 新增证书/专利时间格式校验：`Credential.year` 必须是 `YYYY` 或 `YYYY.MM` |

---

## 3. 核心修改点效果说明

1. **跳转行为与全站统一**：首页「联系我」不再是 `mailto:` 直发邮件，而是与导航栏最右侧入口一样做站内跳转；当前页跳转、不新开标签页，用户进入独立的联系我页面（含邮箱/微信/电话三张卡片与简历下载入口）。
2. **任期口径单一来源**：班长任期只在 `about.ts` 维护一处，关于页与机器人（规则回答 + RAG 上下文）共用同一字段，不可能出现两处不一致。
3. **职务/证书进入机器人能力范围**：新增两条规则后，即使**未配置 API 密钥**（纯规则模式），问「学生工作」「证书资质」也能得到含任期/取证时间的直答；同时两类数据进入 RAG 上下文，API 模式下同样可答。
4. **排名移除但绩点保留**：GPA 数据保留、排名删除，首页教育模块与简历页共用 `Education` 渲染路径，两处自动同步。
5. **GPA 视觉弱化**：GPA 使用 `var(--muted)` 独立 span，字号继承同级 `text-sm`、无 `font-bold`，视觉权重低于学位与专业文字，符合「不放大、不加粗」。

---

## 4. 实测验收记录

运行时实测（本地生产服务器端口 3325，测毕已关闭）

### 4.1 页面

| 检查项 | 实测 |
| --- | --- |
| 首页 hero「联系我」 | `<a class="…" href="/zh/contact">`（en 为 `/en/contact`）；hero 内 `mailto:` = 0；无 `target="_blank"` |
| 导航栏「联系我」 | `href="/zh/contact"`（与首屏按钮一致） |
| `/zh/about`、`/en/about` | 含 `2019.09 – 2022.12`；旧值 `2020.09 – 2023.06` 不存在 |
| `/zh/honors`、`/en/honors` | 证书条目含 `2021.03` |
| 首页 / 关于页 / 简历页 | `4/19`、`1/200` 均不存在；GPA 显示 `GPA 3.6/4.0`、`GPA 3.9/4.0`，样式为 `color:var(--muted)` |

### 4.2 求职信息助理（规则引擎，零 API 调用）

| 提问 | 实测回答（节选） |
| --- | --- |
| `请介绍你的学生工作经历` | `实践经历：`【本科阶段】`• 班级班长（2019.09 – 2022.12）` `• “银杉特训队”社团社长 …（2021.06 – 2022.06）`【硕士阶段】`• 工厂化东星斑养殖智能管理项目（2025.12 – 至今 · 驻场出差负责人 · 文昌冯家湾）` |
| `有哪些证书资质？` | `证书资质：• 全国计算机等级考试二级（C 语言程序设计）（2021.03）• 大学英语四级（CET-4）` |
| `有哪些荣誉资质？` | 仍为「按级别分类」的荣誉回答（未被「资质」关键词劫持，见下） |
| `请介绍你的教育经历` | `• 工学硕士：海南大学 · 新一代电子信息技术` `• 工学学士：四川工业科技学院 · 电子信息工程`（无排名） |

**4.1 附带修复的一个规则优先级问题**：新增「证书资质」规则时，「资质」关键词一度排在「荣誉资质」之前，导致快捷按钮「荣誉资质」被劫持到证书回答（契约立即报 `honors-answer-missing-item`）。已把证书规则移到荣誉规则**之后**，现已恢复：`荣誉资质` → 荣誉回答；`证书资质 / 有哪些证书` → 证书回答。

### 4.3 英文页

`/en`、`/en/about`、`/en/honors`、`/en/resume`、`/en/projects`、`/en/contact` 可见文本汉字 **0**、中文标点 **0**。

---

## 5. 全量校验结果汇总

命令：`npm run verify`

| 步骤 | 结果 |
| --- | --- |
| `verify:content` | ✅ passed（68 个公开文本文件、21 个本地静态资源、`resume.pdf` 完成文本审计） |
| `verify:profile` | ✅ passed（3 projects / 3 research areas / 2 publications / 1 patent / 4 awards / 2 competitions / 5 credentials / 2 practice phases；含新增的证书时间格式校验） |
| `verify:chat` | ✅ passed（**26** quick prompts, 3 public projects；含职务任期、证书时间、教育无排名断言） |
| `verify:resume` | ✅ passed（双语路由、本地 PDF 下载、PDF 隐私审计、隐私边界） |
| `eslint` | ✅ 无输出（0 error / 0 warning） |
| `tsc --noEmit` | ✅ 零错误 |
| `next build` | ✅ 28/28 静态页面生成成功 |

---

## 6. 本地预览验证路径与检查要点

```bash
npm run dev          # http://localhost:3000
```

| 路径 | 检查要点 |
| --- | --- |
| `/zh`、`/en` 首屏 | 点「联系我 / Contact」→ 当前页跳转到 `/[lang]/contact`（不是打开邮件客户端）；按钮样式与图标与改前一致 |
| `/zh/about`、`/en/about` →「实践经历」 | 本科阶段第一条为 `班级班长 / Class Monitor（2019.09 – 2022.12）`；硕士阶段项目任期不变 |
| `/zh/honors`、`/en/honors` →「证书与专利」 | 计算机二级条目显示 `… · 2021.03` |
| `/zh`（教育经历）、`/zh/about`（教育经历）、`/zh/resume`（教育经历） | 均显示 `GPA 3.6/4.0` / `GPA 3.9/4.0`，**无括号排名**；GPA 颜色为次级色、与专业文字同字号 |
| 右下角 AI 助理 | 问「学生工作」→ 输出实践经历（含班长任期）；问「证书资质」→ 输出证书与取证时间；问「荣誉资质」→ 仍输出分级荣誉列表；问「教育经历」→ 无排名 |
| 移动端（约 390px） | 首屏按钮组换行正常；教育经历卡片文字不溢出 |
| 打印预览（简历页） | Ctrl+P 检查 GPA 行与整体版面无变化（本次未改动简历页结构） |

---

## 7. 偏差与残留事项（需你留意）

1. **首页底部「联系我」模块保留**
   任务 1.1 说「不新增首页底部联系模块」——该模块**早已存在**（邮箱/微信/电话三张卡片，上一轮按你「所有包含联系方式的模块统一三项」的要求开启了电话）。本次未新增、也未删除它。如果你希望首页不再重复展示联系方式（只保留按钮跳转），告诉我一声即可移除。

2. **首页「联系我」按钮的 `aria-label` 已移除**
   原按钮是 `mailto:` 链接，带有「发送邮件至公开求职邮箱 …」的无障碍标签；改为站内跳转后该标签会误导读屏用户，因此移除，使其与导航栏「联系我」入口完全一致（仅有可见文字）。按钮的**可见文字、图标与样式类名都未改动**。

3. **证书时间采用 `2021.03` 而非任务书里的英文 `Mar. 2021`**
   全站经历类时间统一为 `YYYY.MM`（任务 2.1 也强调「时间格式与全站经历类信息完全统一」），且 `Credential.year` 是单语言字符串。若你希望英文侧显示月份名，需要把 `Credential.year` 改为双语结构（会影响荣誉页渲染与专利年份校验），请告知。

4. **`CET-4` 仍无取证时间**
   数据层没有该时间，且我不会编造。因此证书回答里 CET-4 只显示名称、不带日期（NCRE 已带）。如你提供时间，我可以一并补上。

5. **简历页仍无「证书」板块**
   任务 3.2 提到「简历页证书板块」，但简历页现有板块为 教育经历 / 项目经历 / 核心技能 / 荣誉奖项 / 专利——**证书未在简历页展示**（只在 `/[lang]/honors` 的「证书与专利」分组）。本次按你上一问的口径（未新增板块）处理；如需在简历页加一个「证书」板块请告知（会增加打印长度）。

6. **机器人实践经历回答包含硕士阶段项目条目**
   因为该数据在 `about.practice` 中与本科学生工作同属「实践经历」。如果你希望机器人只回答学生职务（不含驻场项目），我可以按阶段过滤。

7. **工作区含多轮未提交改动**：本次未创建 commit。根目录现有 12 个未跟踪文件（7 份历史报告 + 4 个组件/模块 + 1 个新目录项），历史报告可按需删除。
